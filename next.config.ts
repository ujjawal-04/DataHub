import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['images.unsplash.com','images.javatpoint.com','images.deepai.org','datastructures.maximal.io','platform.text.com','cdn.programiz.com','i.imgur.com'],
  }
};

export default nextConfig;
